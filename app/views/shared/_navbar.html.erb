<nav class="navbar-wagon navbar-wagon-white">
  <div class="container navbar-wagon-container">
    <div class='navbar-wagon-side'>
      <%= render 'shared/navbar_dropdown', full_item: false %>
      <div class="left hidden-sm hidden-xs">
        <div class="navbar-wagon-item has-no-padding">
          <div class="dropdown">
            <%= link_to t(".menu.cities"),"#", class: "hidden-xs  dropdown-toggle navbar-wagon-link ", id: "navbar-wagon-menu", data: {text: t(".menu.cities"), toggle: 'dropdown'} %>
            <ul class="dropdown-menu dropdown-menu-right dropdown-cities">
              <% Static::CITIES.each do |slug, infos| %>
                <li>
                  <%= link_to infos[:name], city_path(city: slug, locale: locale) %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
        <hr class='hidden-xs'>
        <%= link_to t(".menu.fullstack", default: "Programme"), send(:"program_#{locale}_path"), class: " hidden-xs navbar-wagon-link ", data: {text: t(".menu.fullstack", default: "Programme")} %>
        <hr class='fake-hr hidden-sm hidden-xs'>
      </div>
      </div>
      <%= link_to root_path(locale: locale), class: 'wagon-link' do %>
        <%= render "shared/logos/logo_red_square" %>
      <% end %>
      <div class="navbar-wagon-side text-right hidden-sm hidden-xs">
        <hr class='fake-hr hidden-xs'>
        <%= link_to t(".alumni", default: "Alumni"), alumni_path(locale: locale), class: " hidden-xs navbar-wagon-link", data: {text: t(".alumni", default: "Alumni")} %>
        <hr class='hidden-xs'>
        <%= link_to t(".stories"), story_path(github_nickname: "olixier", locale: locale), class: " hidden-xs navbar-wagon-link", data: {text: t(".stories")} %>
        <div class="left">
          <%= link_to t(".apply"), send(:"apply_#{locale}_path"), class: 'navbar-wagon-link-cta' %>
        </div>
      </div>
    </div>
  </div>
</nav>
<div class="header-fixed">
  <div class="header-inner">
    <div class="container flex align-center">
      <%= render 'shared/navbar_dropdown', full_item: true %>
      <div class='header-fixed-logo'>
        <a class='wagon-link' href="/">
          <%= render "shared/logos/logo_red_square" %>
        </a>
      </div>
      <div class="header-inner-cta">
        <%= link_to t(".apply"), send(:"apply_#{locale}_path"), class: 'navbar-wagon-link-cta' %>
      </div>
    </div>
  </div>
</div>
